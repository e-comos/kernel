# QEMU testing configuration
set default=0
set timeout=2

# Disable graphical menu for serial output
serial --speed=115200
terminal_input serial console
terminal_output serial console

menuentry "Custom OS in QEMU" {
    echo "Loading kernel for QEMU testing..."
    multiboot2 /boot/kernel.elf console=serial
    boot
}
